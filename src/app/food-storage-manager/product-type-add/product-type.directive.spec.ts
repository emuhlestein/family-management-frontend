import { ProductTypeDirective } from './product-type.directive';

describe('ProductTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductTypeDirective();
    expect(directive).toBeTruthy();
  });
});
